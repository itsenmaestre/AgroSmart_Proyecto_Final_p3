<Page x:Class="AGROSMART_GUI.Views.Admin.CosechasPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Gestión de Cosechas"
      Background="#F5F5F5">

    <Page.Resources>
        <!-- Paleta AgroSmart -->
        <Color x:Key="PrimaryColor">#2E7D32</Color>
        <Color x:Key="PrimaryDarkColor">#1B5E20</Color>
        <Color x:Key="PrimaryLightColor">#4CAF50</Color>
        <Color x:Key="AccentColor">#FFC107</Color>
        <Color x:Key="BackgroundColor">#F5F5F5</Color>
        <Color x:Key="CardColor">#FFFFFF</Color>
        <Color x:Key="TextPrimaryColor">#212121</Color>
        <Color x:Key="TextSecondaryColor">#757575</Color>
        <Color x:Key="BorderColor">#E0E0E0</Color>

        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDarkColor}"/>
        <SolidColorBrush x:Key="PrimaryLightBrush" Color="{StaticResource PrimaryLightColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="CardBrush" Color="{StaticResource CardColor}"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>

        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.08" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Section Title -->
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <!-- Form Label -->
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Stretch"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <!-- DatePicker Style -->
        <Style TargetType="DatePicker">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <!-- Primary Button -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryDarkBrush}"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Small Action Button -->
        <Style x:Key="SmallActionButton" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid Style -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F0F0F0"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFAFA"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,12"/>
            <Setter Property="BorderBrush" Value="#F0F0F0"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E8F5E9"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F5F5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <Border Grid.Row="0" Style="{StaticResource CardStyle}" Padding="24,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Width="48" Height="48" CornerRadius="12" 
                            Background="#E8F5E9" Margin="0,0,16,0">
                        <TextBlock Text="🌾" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Gestión de Cosechas" FontSize="24" FontWeight="Bold" 
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Registra y administra las cosechas de tus cultivos" 
                                   FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- FORMULARIOS -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.1*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="1.4*"/>
                </Grid.ColumnDefinitions>

                <!-- DATOS DE LA COSECHA -->
                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <Border Width="36" Height="36" CornerRadius="8" Background="#E8F5E9" Margin="0,0,12,0">
                                <TextBlock Text="📋" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Datos de la Cosecha" Style="{StaticResource SectionTitle}" 
                                       VerticalAlignment="Center" Margin="0"/>
                        </StackPanel>

                        <!-- Cultivo -->
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Cultivo / Lote" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="cboCultivo"/>
                        </StackPanel>

                        <!-- Fechas -->
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Fecha Inicio Cosecha" Style="{StaticResource FormLabel}"/>
                                <DatePicker x:Name="dpFechaInicio"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Fecha Registro" Style="{StaticResource FormLabel}"/>
                                <DatePicker x:Name="dpFechaRegistro" IsEnabled="False"/>
                            </StackPanel>
                        </Grid>

                        <!-- Unidad / Calidad -->
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Unidad de medida" Style="{StaticResource FormLabel}"/>
                                <ComboBox x:Name="cboUnidad">
                                    <ComboBoxItem Content="KG"/>
                                    <ComboBoxItem Content="LB"/>
                                    <ComboBoxItem Content="CAJA"/>
                                    <ComboBoxItem Content="POTE"/>
                                    <ComboBoxItem Content="GALON"/>
                                    <ComboBoxItem Content="RACIMO"/>

                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Calidad" Style="{StaticResource FormLabel}"/>
                                <ComboBox x:Name="cboCalidad">
                                    <ComboBoxItem Content="Alta"/>
                                    <ComboBoxItem Content="Media"/>
                                    <ComboBoxItem Content="Baja"/>
                                </ComboBox>
                            </StackPanel>
                        </Grid>

                        <!-- Cantidades -->
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Cantidad total" Style="{StaticResource FormLabel}"/>
                                <TextBox x:Name="txtCantidad" IsReadOnly="True" Background="#FAFAFA"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Resumen" Style="{StaticResource FormLabel}"/>
                                <TextBox x:Name="txtCantidadTotal" IsReadOnly="True" Background="#FAFAFA"/>
                            </StackPanel>
                        </Grid>

                        <!-- Observaciones -->
                        <StackPanel Margin="0,0,0,20">
                            <TextBlock Text="Observaciones" Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="txtObservaciones" Height="80" TextWrapping="Wrap" 
                                     AcceptsReturn="True" VerticalContentAlignment="Top" Padding="10,8"/>
                        </StackPanel>

                        <!-- Botones -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="btnGuardar" Content="💾  Registrar Cosecha" 
                                    Style="{StaticResource PrimaryButtonStyle}" Click="BtnGuardar_Click"/>
                            <Button Content="🧹  Limpiar" Style="{StaticResource SecondaryButtonStyle}" 
                                    Margin="12,0,0,0" Click="BtnLimpiar_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- DETALLE DE EMPLEADOS -->
                <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                            <Border Width="36" Height="36" CornerRadius="8" Background="#FFF3E0" Margin="0,0,12,0">
                                <TextBlock Text="👥" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Detalle de Empleados (por día)" Style="{StaticResource SectionTitle}" 
                                       VerticalAlignment="Center" Margin="0"/>
                        </StackPanel>

                        <!-- Formulario Empleado -->
                        <StackPanel Grid.Row="1">
                            <StackPanel Margin="0,0,0,14">
                                <TextBlock Text="Empleado" Style="{StaticResource FormLabel}"/>
                                <ComboBox x:Name="cboEmpleado"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,0,14">
                                <TextBlock Text="Fecha de trabajo" Style="{StaticResource FormLabel}"/>
                                <DatePicker x:Name="dpFechaTrabajo"/>
                            </StackPanel>

                            <!-- Cantidad / Valor Unitario -->
                            <Grid Margin="0,0,0,14">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Cantidad" Style="{StaticResource FormLabel}"/>
                                    <TextBox x:Name="txtCantidadEmpleado" Text="0.00" MinWidth="100"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Valor unitario" Style="{StaticResource FormLabel}"/>
                                    <TextBox x:Name="txtValorUnitario" Text="0" MinWidth="100"/>
                                </StackPanel>
                            </Grid>

                            <!-- NUEVO: Deducciones / Porcentaje -->
                            <Grid Margin="0,0,0,14">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Style="{StaticResource FormLabel}">
            <Run Text="Deducciones"/>
            <Run Text=" (opcional)" FontSize="11" Foreground="#999"/>
                                    </TextBlock>
                                    <TextBox x:Name="txtDeducciones" Text="0" MinWidth="100"
                 ToolTip="Monto fijo de deducciones. Dejar en 0 para usar el porcentaje automático."/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="% Deducción" Style="{StaticResource FormLabel}"/>
                                        <TextBlock Text=" (por defecto 5%)" FontSize="11" Foreground="#999" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <TextBox x:Name="txtPorcentajeDeduccion" Text="5" MinWidth="100"
                 ToolTip="Porcentaje de deducción si no se especifica un monto fijo."/>
                                </StackPanel>
                            </Grid>

                            <StackPanel Margin="0,0,0,16">
                                <TextBlock Text="Observaciones" Style="{StaticResource FormLabel}"/>
                                <TextBox x:Name="txtObservacionesEmpleado"/>
                            </StackPanel>

                            <Button Content="➕  Agregar / Actualizar" Style="{StaticResource PrimaryButtonStyle}" 
                                    HorizontalAlignment="Right" Click="BtnAgregarEmpleado_Click" Margin="0,0,0,20"/>
                        </StackPanel>

                        <!-- DataGrid Empleados -->
                        <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8">
                            <DataGrid x:Name="dgEmpleados" MaxHeight="250">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Empleado" Binding="{Binding NombreEmpleado}" Width="*" MinWidth="120"/>
                                    <DataGridTextColumn Header="Fecha" Binding="{Binding FechaTrabajo, StringFormat=dd/MM/yyyy}" Width="95"/>
                                    <DataGridTextColumn Header="Cantidad" Binding="{Binding CantidadCosechada}" Width="80"/>
                                    <DataGridTextColumn Header="V. Unitario" Binding="{Binding ValorUnitario}" Width="85"/>
                                    <DataGridTextColumn Header="Observaciones" Binding="{Binding Observaciones}" Width="*" MinWidth="100"/>
                                    <DataGridTemplateColumn Header="" Width="55">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="🗑" ToolTip="Eliminar" Background="#FFEBEE" 
                                                        Foreground="#D32F2F" Style="{StaticResource SmallActionButton}"
                                                        Padding="8,0" Tag="{Binding}" Click="BtnEliminarEmpleado_Click"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </Grid>
                </Border>
            </Grid>

            <!-- HISTORIAL -->
            <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                        <Border Width="36" Height="36" CornerRadius="8" Background="#E3F2FD" Margin="0,0,12,0">
                            <TextBlock Text="📊" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Cosechas activas" Style="{StaticResource SectionTitle}" 
                                   VerticalAlignment="Center" Margin="0"/>
                    </StackPanel>

                    <Border Grid.Row="1" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8">
                        <DataGrid x:Name="dgCosechas" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding IdCosecha}" Width="50"/>
                                <DataGridTextColumn Header="Cultivo / Lote" Binding="{Binding NombreCultivo}" Width="*"/>
                                <DataGridTextColumn Header="Fecha Inicio" Binding="{Binding FechaInicio, StringFormat=dd/MM/yyyy}" Width="110"/>
                                <DataGridTextColumn Header="Cantidad" Binding="{Binding CantidadObtenida}" Width="90"/>
                                <DataGridTextColumn Header="Unidad" Binding="{Binding UnidadMedida}" Width="80"/>
                                <DataGridTextColumn Header="Calidad" Binding="{Binding Calidad}" Width="90"/>
                                <DataGridTemplateColumn Header="Estado" Width="110">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="12" Padding="10,4" HorizontalAlignment="Left">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#FFF3E0"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Estado}" Value="TERMINADA">
                                                                <Setter Property="Background" Value="#E8F5E9"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding Estado}" FontSize="11" FontWeight="SemiBold">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#E65100"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Estado}" Value="TERMINADA">
                                                                    <Setter Property="Foreground" Value="#2E7D32"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="Acciones" Width="220">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Content="👁 Ver" ToolTip="Ver detalles" Background="#E3F2FD" 
                                                        Foreground="#1976D2" Style="{StaticResource SmallActionButton}"
                                                        Padding="10,0" Tag="{Binding}" Click="BtnVer_Click" Margin="0,0,4,0"/>
                                                <Button Content="✏️ Editar" ToolTip="Editar cosecha" Background="#FFF3E0" 
                                                        Foreground="#F57C00" Style="{StaticResource SmallActionButton}"
                                                        Padding="10,0" Tag="{Binding}" Click="BtnEditar_Click" Margin="0,0,4,0"/>
                                                <Button Content="✓ Finalizar" ToolTip="Finalizar cosecha" Background="#C8E6C9" 
                                                        Foreground="#2E7D32" Style="{StaticResource SmallActionButton}"
                                                        Padding="10,0" Tag="{Binding}" Click="BtnTerminarCosecha_Click"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
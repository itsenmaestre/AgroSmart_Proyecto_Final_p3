<Window x:Class="AGROSMART_GUI.Views.Empleado.RegistrarAvanceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Registrar Avance de Tarea" 
        Height="743" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>

        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="10,0">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          VerticalAlignment="Center"
                                          Focusable="false"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ButtonPrimaryStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                Padding="15,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#45A049"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ButtonSecondaryStyle" TargetType="Button">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                Padding="15,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D0D0D0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="10" Margin="20" Padding="30">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.3"/>
        </Border.Effect>

        <StackPanel>
            <!-- Título -->
            <TextBlock Text="📝 Registrar Avance de Tarea" 
                       FontSize="22" FontWeight="Bold" 
                       Foreground="#4CAF50" Margin="0,0,0,30"/>

            <!-- Información de la tarea -->
            <Border Background="#F5F5F5" CornerRadius="5" Padding="15" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock x:Name="txtInfoTarea" 
                               Text="Tarea: Riego de cultivo #1234" 
                               FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                    <TextBlock x:Name="txtFechaTarea" 
                               Text="Fecha programada: 15 de marzo, 2025" 
                               FontSize="12" Foreground="#666" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Formulario de registro -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Horas trabajadas -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Horas trabajadas" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="txtHorasTrabajadas" 
                             Style="{StaticResource InputStyle}"
                             Text="0.00"/>
                    <TextBlock Text="Ejemplo: 8 horas o 8.5 horas" 
                               FontSize="11" Foreground="#999" Margin="0,3,0,0"/>
                </StackPanel>

                <!-- Jornadas trabajadas -->
                <StackPanel Grid.Row="1" Margin="0,0,0,20">
                    <TextBlock Text="Jornadas trabajadas" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="txtJornadasTrabajadas" 
                             Style="{StaticResource InputStyle}"
                             Text="0.00"/>
                    <TextBlock Text="Número de días completos trabajados" 
                               FontSize="11" Foreground="#999" Margin="0,3,0,0"/>
                </StackPanel>

                <!-- Estado de la tarea -->
                <StackPanel Grid.Row="2" Margin="0,0,0,20">
                    <TextBlock Text="Estado de la tarea" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="cboEstado" Height="35" FontSize="14">
                        <ComboBoxItem Content="PENDIENTE" IsSelected="True"/>
                        <ComboBoxItem Content="EN_EJECUCION"/>
                        <ComboBoxItem Content="FINALIZADA"/>
                    </ComboBox>
                </StackPanel>

                <!-- Observaciones -->
                <StackPanel Grid.Row="3" Margin="0,0,0,20">
                    <TextBlock Text="Observaciones (opcional)" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="txtObservaciones" 
                             Height="80" 
                             Padding="10"
                             FontSize="14"
                             BorderBrush="#CCCCCC"
                             BorderThickness="1"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Text=""/>
                </StackPanel>
            </Grid>

            <!-- Botones de acción -->
            <Grid Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Cancelar" 
                        Style="{StaticResource ButtonSecondaryStyle}"
                        Click="BtnCancelar_Click"/>

                <Button Grid.Column="2" Content="Guardar Avance" 
                        Style="{StaticResource ButtonPrimaryStyle}"
                        Click="BtnGuardar_Click"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>

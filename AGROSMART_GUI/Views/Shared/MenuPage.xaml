<Page x:Class="AGROSMART_GUI.Views.Shared.MenuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent"
      Title="MenuPage">

    <Page.Resources>

        <!-- ANIMACIÓN SUAVE AL PASAR EL MOUSE -->
        <Style x:Key="HoverScale" TargetType="Border">
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                             To="1.06" Duration="0:0:0.15"/>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                             To="1.06" Duration="0:0:0.15"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>

                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                             To="1" Duration="0:0:0.15"/>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                             To="1" Duration="0:0:0.15"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <!-- BOTÓN PRINCIPAL -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#00A859"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="55"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="12" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#00C068"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="18"
                                                          ShadowDepth="2"
                                                          Color="#22000000"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#008A47"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Page.Resources>


    <!-- FONDO GENERAL -->
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#FDFDFD" Offset="0"/>
                <GradientStop Color="#F1F6F3" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <!-- TARJETA CENTRAL -->
        <Border Width="450" Padding="50"
                Background="White"
                CornerRadius="20"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Style="{StaticResource HoverScale}">
            <Border.Effect>
                <DropShadowEffect BlurRadius="25"
                                  ShadowDepth="4"
                                  Opacity="0.15"/>
            </Border.Effect>

            <StackPanel HorizontalAlignment="Center">

                <!-- ICONO -->
                <Border Background="#00A859" Width="110" Height="110"
                        CornerRadius="55" Margin="0,0,0,30">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="20"
                                          ShadowDepth="3"
                                          Opacity="0.25"/>
                    </Border.Effect>
                    <TextBlock Text="🌿" FontSize="50"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>

                <!-- TÍTULO -->
                <TextBlock Text="¡Bienvenido!"
                           FontSize="44"
                           FontWeight="Black"
                           Foreground="#00A859"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="AgroSmart te acompaña en la gestión agrícola"
                           FontSize="17"
                           Foreground="#4E6E55"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,45"/>

                <!-- BOTÓN -->
                <Button Content="Ingresar a mi cuenta"
                        Style="{StaticResource PrimaryButton}"
                        Click="IngresarButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>

</Page>

